以后当我明确说“整理笔记”时，请严格按照以下格式输出：

# 一、显示形式

1. 必须把整篇笔记放在一个白色可编辑的 Writing Block 中。
2. 这个 Writing Block 应带有编辑、复制、下载、全屏等按钮。
3. 不要使用普通聊天正文代替。
4. 不要使用 Canvas。
5. 不要生成需要我另外下载的文档文件。
6. Writing Block 内只放真正需要复制进学习笔记的正文内容。
7. “放置目录、分类建议、为什么放在该目录、补充说明、对笔记内容的额外解释”等不属于笔记正文的内容，不要放进 Writing Block。
8. 如果我在要求“整理笔记”的同时，还询问了放置目录、分类位置或其他说明，这些内容必须与 Writing Block 分开，放在 Writing Block 外；Writing Block 中仍然只保留笔记正文。
9. 可以使用下面这种结构输出：

:::writing{id="唯一英文ID" variant="standard" title="笔记标题"}
笔记全部内容
:::

# 二、通用要求

1. 笔记标题只写我让你整理的函数、宏、数据类型或结构体名称。
2. 内容要简洁，适合直接复制到我的学习笔记中。
3. 不要主动加入过多原理、背景知识、注意事项或扩展内容。
4. 我需要详细解释时会另外问你。
5. 所有函数、宏、数据类型和结构体笔记，都必须写明需要包含的头文件。
6. 整体介绍可以写的详细一些。
7. 不要擅自删掉我要求保留的内容。
8. 一次整理多个项目时，每个项目必须单独生成一篇笔记，不要合并在同一个标题下。
9. 名称、参数类型、返回值和头文件必须准确。
10. 不确定的内容不要编造。
11. 正文里的结构体名、函数名、宏名都直接写普通文本，不用行内代码样式
12. 头文件不要单独设置“必需头文件”“头文件”等标题，应直接和函数原型、结构体原型、宏定义等内容放在同一个 C 代码块中。
13. 如非必要，不要擅自增加小标题。
14. “作用”“结构体原型”“重点成员”“核心理解”“成员说明”等内容，默认都不要设置成单独标题，直接使用普通正文、代码块或表格进行说明即可。
15. 只有在内容较多、确实需要分组才能保证结构清晰时，才允许增加必要的小标题。
16. 我指定的标题等级只用于当前需要整理的函数、宏、数据类型或结构体名称，不要在正文中随意增加同级或接近同级的标题。
# 三、宏

对于同一类别、命名规律一致、用途相近的一组宏（例如 EV_*、ABS_MT_*），默认不要把每个宏分别拆成独立小节详细整理。
这类宏应采用“集中列举”的方式整理：
1. 整组宏只使用一个标题。
2. 必需头文件只写一次。
3. 所有宏集中放在同一个 C 代码块中。
4. 每个宏在行尾使用简短注释说明作用。
5. 不需要为每个宏重复写作用说明、头文件和代码示例。
6. 只有当我明确要求详细整理某一个宏时，才单独展开。
7. 函数式宏、参数含义较复杂或需要配合其他函数使用的宏（例如 EVIOCGABS）仍然可以单独详细整理。

# 四、函数笔记格式

1.函数笔记必须严格采用下面的结构
2.标题等级我会传入（例如：整理笔记4），如果我没有传入则直接按照下面结构默认的
3.不要给我要求你整理的一个笔记设置同级标题

### 函数名

```c
/**
 * @brief  函数作用。
 *
 * @param  参数名: 参数含义。
 *
 * @retval 返回值情况1: 含义。
 *
 * @retval 返回值情况2: 含义。
 */
#include <需要的头文件>

函数原型

```

# 四、函数笔记格式

1. 函数笔记必须严格采用下面的结构。
2. 标题等级我会传入（例如：`整理笔记4` 表示使用四级标题 `####`）；如果我没有传入标题等级，则直接按照下面结构中的默认标题等级整理。
3. 不要给我要求整理的一个笔记设置与函数名同级的其他标题。函数名是该篇笔记唯一的同级标题，其他说明直接写正文，不要再增加“作用”“参数说明”“返回值”“注意事项”“示例”等同级小标题。
4. 函数所需的头文件必须和函数原型放在同一个 C 代码框中。
5. 函数注释必须使用 `@brief`、`@param`、`@retval` 的形式：
    - `@brief`：说明函数的主要作用。
    - `@param`：每个参数分别说明参数含义，必要时说明可取值及不同取值的作用。
    - `@retval`：分别列出不同返回值及其含义。
6. 如果某个函数本身没有特殊限制，但在操作某个具体芯片、设备或模块时存在额外要求，应在函数注释代码框中使用 `@note` 补充说明。
    这类额外要求包括但不限于：
    - 写入后需要等待一定时间；
    - 必须按照特定的访问顺序操作；
    - 某些状态下不能访问；
    - 参数受到具体硬件限制；
    - 芯片内部操作尚未完成时不能立即进行下一次操作；
    - 某个具体设备存在特殊的数据格式、时序或使用要求。
7. `@note` 必须明确区分“函数本身的规则”和“具体设备的规则”，不能让人误以为具体芯片的限制是该函数的通用要求。
    例如：

```c
/**
 * @note   当该函数用于 AT24C02 时，写入后芯片还需要进行 EEPROM 内部写操作，
 *         内部写周期 tWR 最大为 10 ms。
 *         简单处理时可延时 10 ms，也可以使用 ACK Polling 判断写入是否完成。
 *
 *         这里的 10 ms 是 AT24C02 芯片本身的要求，
 *         不是该函数本身要求的延时时间。
 */
```

8. 如果没有与具体设备、芯片或特殊使用场景相关的注意事项，则不要为了保持格式而强行添加 `@note`。
9. `@note` 中如果存在更规范、更推荐的处理方法，可以在说明简单做法的同时简要补充。例如“固定延时”之外还可以使用“ACK Polling”等方式。
10. 函数笔记默认结构如下：
#### 函数名

```c
/**
 * @brief  函数作用。
 *
 * @param  参数名: 参数含义。
 *
 * @retval 返回值情况1: 含义。
 *
 * @retval 返回值情况2: 含义。
 *
 * @note   与具体芯片、设备或特殊场景有关的注意事项。
 *         如果没有，则删除整个 @note 部分。
 */
#include <需要的头文件>

函数原型
```

代码框下面可以继续补充理解函数所必需的说明和简洁示例，但不要重复代码框中已经明确说明的内容，也不要添加与当前函数无关的扩展内容。